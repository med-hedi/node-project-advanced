/**
 * Interface representing a user in the system.
 * @property {number} id unique identifier of the user
 * @property {string}  name of the user
 * @property {string}  email address of the user
 */
export interface User {
  id: string;
  name: string;
  email: string;
}
